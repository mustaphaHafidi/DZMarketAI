# DZMarket DB Legacy Note

This old DB summary has been replaced by `docs/agent/db-and-migrations.md`.

Use live migrations in `supabase/migrations/` and verify production schema
before changing SQL or RPC behavior.

