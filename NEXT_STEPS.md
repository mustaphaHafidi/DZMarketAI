# NEXT_STEPS

This legacy planning file is no longer the default task source.

Use:

- `docs/agent/README.md` for routing.
- `docs/agent/known-issues.md` for current cautions.
- `docs/agent/qa-regression.md` before code or release work.
- `docs/agent/mobile-release.md` for Android/iOS deployment.

Before acting, verify live repo and production/store state.
